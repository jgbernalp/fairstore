@import '../ui/Theme.styl';

.img {
  width: 100%;
  height: 120px;
  object-fit: cover;
  display: block;
}

.title {
  color: t_secondary;
}

.container {
  padding: 1rem;
  display: flex;
  align-items: center;
}